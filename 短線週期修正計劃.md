# 短線週期法功能新增計劃

## 概述

在短線過濾掃描區域新增第四個功能「**短線週期法**」。用戶先用月曆選擇歷史日期（共用現有 `ShortTermCalendarModal`），然後點擊「短線週期掃描」按鈕，系統以**當日（今天）**作為計算基礎，對選擇的歷史股票輸出「進場 / 出場 / 持倉」三種狀態。

> 此功能完全獨立，不修改既有三個掃描按鈕或其 State、API。

---

## 核心邏輯（依據 短線週期法.md）

數據來源：FinMind 台股日K線 (TWStockPrice / TWSEStockPriceOTC)

### 進場訊號 (BUY)
同時滿足以下三個條件：
- **條件 A**：今日收盤 > 前 20 個交易日最高價（收盤突破，非盤中）
- **條件 B**：今日成交量 >= 5日均量 x 1.5
- **條件 C**：今日成交量 > 1,000 張（流動性門檻）

### 出場訊號 (EXIT)
滿足以下任一：
- **條件 D**：今日收盤 < 過去 10 日最低價（移動停損）
- **條件 E**：今日收盤 < 歷史掃描日當天的日K最低點
  （以歷史掃描日作為假設的進場日，直接向 FinMind 查詢各股票在歷史掃描日的日K，取當天 Low 作為防守線）

### 持倉 (HOLD)
進場條件與出場條件皆未觸發。

---

## 新增檔案清單

### 1. [NEW] `src/app/api/scan/cycle-method/route.ts`
接受 `POST { stockIds: string[], entryDates: Record<string, string>, market: string }` 請求：
- 用 FinMind 批次查詢每支股票近 25 個交易日 K 線
- 計算條件 A / B / C / D / E
- 若數據不足 20 天則自動跳過（回傳 HOLD）
- 回傳 `{ results: { stock_id, stock_name, signal, close, changePercent, reason, entryLow }[] }`

### 2. [NEW] `src/components/CycleMethodModal.tsx`
顯示「短線週期掃描」結果彈出視窗，分三個分組卡片：
- 🟢 **進場** (BUY)：列出所有觸發進場訊號的股票
- 🔴 **出場** (EXIT)：列出所有觸發出場訊號的股票
- ⚪ **持倉** (HOLD)：列出無訊號的股票

### 3. [MODIFY] `src/app/page.tsx`
新增獨立 State：
```ts
const [isCycleScanning, setIsCycleScanning] = useState(false);
const [cycleResults, setCycleResults] = useState<CycleResult[]>([]);
const [showCycleModal, setShowCycleModal] = useState(false);
```

在現有三個掃描按鈕下方新增一行「🔄 短線週期掃描」按鈕（綠色系）。
只有在 `shortTermSelectedDates.size > 0` 時才可點擊，否則顯示說明文字。

---

## 不影響清單

| 功能 | 涉及的 State/API | 是否修改 |
|------|-----------------|---------|
| 定點共振掃描 | `results`, `isWorking` | 不動 |
| 強化評分掃描 | `enhancedResults`, `isEnhancedScanning` | 不動 |
| 短線過濾掃描 | `shortTermResults`, `isShortTermScanning` | 不動 |
| 歷史月曆 UI | `ShortTermCalendarModal`, `shortTermSelectedDates` | 共用，不修改 |
| Firebase 儲存 | `saveScanRecord` | 不動（週期法結果不儲存）|

---

## 防守線設計（條件 E）

直接向 FinMind 查詢各股票在歷史掃描日的日K線，取那天的 Low 作為絕對防守線。
此查詢在 API Route 內進行，不影響前端邏輯。
